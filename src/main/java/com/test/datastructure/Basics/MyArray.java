package com.test.datastructure.Basics;

/**
 * new MyArray();
 * new MyArray(int size);
 * int length();            // 获取数组长度
 * void put(T element);     // 插入元素、修改元素
 * void delete(T element);  // 删除元素
 * T get(int index);        // 获取元素
 */
public class MyArray {

}
